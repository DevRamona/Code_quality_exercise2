// function greet(name) {
//     if (name) {
//       return "Hello, " + name + "!";
//     } else {
//       return "Hello!";
//     }
//   }

const greeting = (name) => {
    return (name) ? `Hello, ${name}!` : "Hello!"
}
